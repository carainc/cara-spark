# Flat node_modules (npm-style) so the Prisma client + query-engine resolve at node_modules/.prisma
# and node_modules/@prisma/client — required for the Next standalone Docker image (Dockerfile COPYs)
# and reliable Prisma engine loading. Lockfile is unaffected (node-linker is install-layout only).
node-linker=hoisted
