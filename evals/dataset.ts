import { CASES, type TriageCase } from '@/fixtures/cases';

/** The eval dataset = the synthetic fixtures. Kept separate so T4 can add held-out cases. */
export const evalCases: TriageCase[] = CASES;
