# CLAUDE.md

This project's agent playbook lives in **[AGENTS.md](AGENTS.md)** — read it fully before touching a file.

Resume anchor: **[RUN_STATE.md](RUN_STATE.md)** (+ `took task ready` + the branch). Never re-derive.

Quickstart: `cp .env.example .env` → fill keys → `docker compose up` (or `pnpm install && make dev`).
